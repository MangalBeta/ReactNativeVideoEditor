

import {
    StyleSheet,
  } from 'react-native';
  import colors from '../../constants/colors';
  import {RfH, RfW,Height} from '../../utils/helpers';
  
  const styles = StyleSheet.create({
    emptyContainer :{
        flex: 1, height: Height() / 3,
        justifyContent: 'center', alignItems: 'center'
      }

    })


    export default styles
